import { Cache } from "../../cache/Cache";

/**
 * @class
 * @memberOf next2d.fw
 * @extends {Cache}
 */
export class Query extends Cache
{
    /**
     * @constructor
     * @public
     */
    constructor ()
    {
        super();
    }
}