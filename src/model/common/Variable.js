import { Cache } from "../../cache/Cache";

/**
 * @class
 * @memberOf next2d.fw
 * @extends {Cache}
 */
export class Variable extends Cache
{
    /**
     * @constructor
     * @public
     */
    constructor ()
    {
        super();
    }
}